{% extends "ex00/base.html" %} 
{% block sty %}

.gg {
     border-width:1px;
    border-style:solid;
    border-color:red;

 }
th{
    border-width:1px;
    border-style:solid;
    border-color:black;
}
td{
    border-width:1px;
    border-style:solid;
    border-color:black;
}
{% endblock %}
{% block content %} 
{% include "ex00/nav.html" %}
<h1>Your favorites</h1>
{% if user.is_authenticated %}
<table class='table table-bordered'>
  <th>Title</th>
  <th>Author</th>
  {% for favorite in object_list %}
  <tr>
	<!-- par defaut, le parametre passe dans l'url ci-dessous doit s'appeler pk ou slug -->
	<td><a href="{% url 'detail' pk=favorite.article.id %}">{{ favorite.article.title }}</a></td>
	<td>{{ favorite.user }}</td>
	    </tr>
  {% endfor %}
</table>
{% else %}
Restricted area, please login
{% endif %}

{% endblock %} 
